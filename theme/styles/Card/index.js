const CardStyles = params => {
    const {framework, props} = params;

    return `
        .fab-card {
            background: #FFF;
            border: solid 1px #DDD;
        }
        
        .fab-card-body {
            
        }
    `;
}

export default CardStyles;