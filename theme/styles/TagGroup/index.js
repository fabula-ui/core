const TagGroupStyles = params => {
    return `
        .fab-tag-group {
            display: flex;
            flex-wrap: wrap;
            margin: -.25rem;
        }

        .fab-tag-group .fab-tag-wrapper {
            padding: .25rem;
        }
    `;
}

export default TagGroupStyles;