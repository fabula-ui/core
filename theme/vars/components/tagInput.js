import colors from '../colors';
import globals from '../globals';

const tagInput = {
    ...globals,
    colors,
    minHeight: '3rem',
    padding: '1rem',
    spacing: '.5rem'
}

export default tagInput;