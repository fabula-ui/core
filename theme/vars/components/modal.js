import colors from '../colors';
import globals from '../globals';

const modal = {
    ...globals,
    colors,

    color: '#FFF',
    sizes: {
        sm: '320px',
        md: '480px',
        lg: '640px',
        xl: '800px'
    }
}

export default modal;