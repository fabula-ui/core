import colors from '../colors';
import globals from '../globals';

const tabs = {
    ...globals,
    colors,

    // Component vars
    borderColor: 'light',
    fontSize: '.9rem',
}

export default tabs;