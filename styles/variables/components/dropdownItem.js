import colors from '../colors';
import globals from '../globals';

const dropdownItem = {
    ...globals,
    ...colors,
    
    paddingX: '1rem',
    paddingY: '.5rem'
}

export default dropdownItem;