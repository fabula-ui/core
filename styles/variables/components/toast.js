import colors from '../colors';
import globals from '../globals';

const toast = {
    ...globals,
    colors,

    borderWidth: '1px',
    color: '#FFF',
    fontSize: '.95rem',
    fontWeight: 600,
    glowX: 0,
    glowY: '2px',
    glowRadius: '2px',
    glowSpread: 0,
    iconOffset: '.75em',
    letterSpacing: '-.025em',
    paddingX: '1em',
    paddingY: '1em',
    spacing: '.5rem'
}

export default toast;