import Color from 'color';

import colors from '../colors';
import globals from '../globals';

const toast = {
    ...globals,
    colors,

    fontSize: '.95rem',
    fontWeight: 600,
    letterSpacing: '-.025em',
    paddingX: '1em',
    paddingY: '1em',
    spacing: '.5rem'
}

export default toast;