import colors from '../colors';
import globals from '../globals';

const dropdown = {
    ...globals,
    ...colors,

    borderRadius: '.5rem',
    colors,
    spacing: '.5rem'
}

export default dropdown;