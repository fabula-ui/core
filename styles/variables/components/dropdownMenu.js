import colors from '../colors';
import globals from '../globals';

const dropdownMenu = {
    ...globals,
    ...colors,
    
    color: '#FFF',
    paddingX: 0,
    paddingY: '.5em',
    spacing: '.33em'
}

export default dropdownMenu;